<% if @category.errors.any? %>
  alert('something went wrong')
<% else %>
  $('#categoryTable').append("<%= escape_javascript render('category_row', category: @category) %>")
  $('.btn-close').click()
<% end %>